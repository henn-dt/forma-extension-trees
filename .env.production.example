# =============================================================================
# Production Environment Configuration
# =============================================================================
# URL: https://webapps.henn.com/forma-tree
# 
# Copy this file to .env on your production server and fill in the values.
# 
# SECURITY NOTES:
# - Never commit the actual .env file to git
# - Use strong, unique secrets for SESSION_SECRET
# - Keep tokens secure and rotate them periodically
# =============================================================================

# =============================================================================
# REQUIRED: CORS Configuration
# =============================================================================
# Comma-separated list of origins allowed to access this API.
# 
# MUST include:
# 1. Forma app origin (where extension is embedded): https://app.autodeskforma.eu
# 2. Your app's public URL: https://webapps.henn.com
# =============================================================================
ALLOWED_ORIGINS=https://app.autodeskforma.eu,https://webapps.henn.com

# =============================================================================
# REQUIRED: Session Security
# =============================================================================
# A secure random string for encrypting session cookies (used in auth-setup.js).
# This encrypts user login sessions - MUST be unique and secret!
#
# Generate with: openssl rand -base64 32
# Or in PowerShell: [Convert]::ToBase64String((1..32 | ForEach-Object { Get-Random -Maximum 256 }) -as [byte[]])
# 
# Example (DO NOT USE THIS - generate your own!):
# SESSION_SECRET=K8mN2pQ9xR4wF7hJ1vL5cT3yB6zA0dE8gU2iO4sM7nP
# =============================================================================
SESSION_SECRET=GENERATE_A_SECURE_RANDOM_STRING_HERE

# =============================================================================
# REQUIRED: Directus Configuration
# =============================================================================
# Directus is used for user authentication and project storage.
# =============================================================================
DIRECTUS_URL=https://datastudio.henn.com
DIRECTUS_STATIC_TOKEN=your_directus_static_token_here

# =============================================================================
# OPTIONAL: External Data Protection Policy (runtime-mounted file)
# =============================================================================
# Keep the policy HTML OUT of this repository and OUT of the built image.
# Point this to the file path inside the backend container.
#
# If unset or file is missing, registration works without consent checkbox.
#
# Example with docker-compose.production.yml sample mount:
# DATA_POLICY_FILE_PATH=/run/secrets/data-protection-policy.html
# =============================================================================
# DATA_POLICY_FILE_PATH=/run/secrets/data-protection-policy.html

# =============================================================================
# OPTIONAL: Forma API Tokens
# =============================================================================
# Only needed if the backend makes direct calls to Forma/Autodesk API.
# These tokens expire and need periodic refresh.
# =============================================================================
# BEARER_TOKEN=your_forma_bearer_token
# REFRESH_TOKEN=your_forma_refresh_token
# CLIENT_ID=your_forma_client_id
# CLIENT_SECRET=your_forma_client_secret
